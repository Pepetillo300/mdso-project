{
  "name": "gke-cicd-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal zero-dependency web app used to demonstrate a Cloud Build -> Artifact Registry -> GKE pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
